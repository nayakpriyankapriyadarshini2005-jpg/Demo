const a= 10
const b= 20

const str = " a value is " +a+ " b value is " + b+ " sum of a and b is "+(a+b)
console.log(str)

const templateString = `a value is ${a}, b value is ${b}, sum is ${a+b}`//template literal
console.log(templateString);

